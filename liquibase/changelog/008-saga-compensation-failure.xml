<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

  <changeSet id="801-compensation-failure" author="paypulse">
    <sql>
      <![CDATA[
      CREATE TABLE saga.compensation_failure (
        saga_id    UUID PRIMARY KEY,
        reason     TEXT NOT NULL,
        payload    TEXT,
        created_at TIMESTAMPTZ(3) NOT NULL DEFAULT (timezone('utc', now())),
        resolved   BOOLEAN NOT NULL DEFAULT false
      )
      ]]>
    </sql>
    <sql>CREATE INDEX idx_compensation_failure_unresolved ON saga.compensation_failure (resolved, created_at DESC)</sql>
  </changeSet>

</databaseChangeLog>
