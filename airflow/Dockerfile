FROM apache/airflow:2.8.3-python3.11

USER root
RUN apt-get update \
    && apt-get install -y --no-install-recommends git \
    && rm -rf /var/lib/apt/lists/*

USER airflow
RUN pip install --no-cache-dir "dbt-clickhouse>=1.8.0,<1.9" "clickhouse-connect>=0.7.0"
